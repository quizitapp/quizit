# QuizIt

## <u>Server</u> 
##### Initial setup

1. Cd to server server's folder
`cd server`
<br/>

2. Run the following command to setup the local DB:
`docker compose up -d`
<br/>

3. Run the following command to setup the local DB:
